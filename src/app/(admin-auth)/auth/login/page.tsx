import { LoginForm } from "@/components/auth/login-form"

export const metadata = {
  title: "Login | Gobai",
  description: "Login to your Gobai account",
}

export default function LoginPage() {
  return <LoginForm />
}
